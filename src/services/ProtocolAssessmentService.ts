import { getDebriefReport, getDebriefVersion, subscribeToDebrief } from "@/services/DebriefService"; import { protocolConfigurationRegistry } from "@/services/protocol/ProtocolConfigurationService"; import { assessProtocol } from "@/services/assessment/ProtocolAssessmentEngine";
let key = ""; let cached: ReturnType<typeof assessProtocol> | undefined;
export function getProtocolAssessmentReport() { const debrief = getDebriefReport(); const provenance = debrief.protocolProvenance; if (!provenance) return undefined; const nextKey = `${getDebriefVersion()}:${provenance.protocolHash}`; if (cached && key === nextKey) return cached; const protocol = protocolConfigurationRegistry.require({ protocolId: provenance.protocolId, version: provenance.version }); cached = assessProtocol({ protocol, debrief, timeline: debrief.timeline }); key = nextKey; return cached; }
export const getProtocolAssessmentVersion = () => `${getDebriefVersion()}:${getDebriefReport().protocolProvenance?.protocolHash ?? "NONE"}`; export const subscribeToProtocolAssessment = subscribeToDebrief;
