import type { ProtocolTemporalConstraint } from "@/models/protocol/ClinicalProtocolConfiguration"; import type { ExerciseTimelineEvent } from "@/models/exercise/ExerciseTimelineEvent";
export type TemporalEvaluation = Readonly<{ valid: boolean; malformed: boolean }>;
export function evaluateTemporal(trigger: ExerciseTimelineEvent, action: ExerciseTimelineEvent, constraint?: ProtocolTemporalConstraint): TemporalEvaluation { if (!constraint) return Object.freeze({ valid: action.simulationTimeSec >= trigger.simulationTimeSec, malformed: false }); const delta = action.simulationTimeSec - trigger.simulationTimeSec; if (constraint.relation === "AFTER") return Object.freeze({ valid: delta >= 0, malformed: false }); if (constraint.relation === "BEFORE") return Object.freeze({ valid: delta <= 0, malformed: false }); if (constraint.relation === "WITHIN") { if (!Number.isFinite(constraint.durationSec) || Number(constraint.durationSec) <= 0) return Object.freeze({ valid: false, malformed: true }); return Object.freeze({ valid: delta >= 0 && delta <= Number(constraint.durationSec), malformed: false }); } return Object.freeze({ valid: false, malformed: true }); }
