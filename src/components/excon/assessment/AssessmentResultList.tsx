import type { ProtocolAssessmentResult } from "@/models/assessment/ProtocolAssessment"; import { Pressable, StyleSheet, Text, View } from "react-native";
export function AssessmentResultList({ results, selectedId, onSelect }: { results: readonly ProtocolAssessmentResult[]; selectedId?: string; onSelect(id: string): void }) { return <View>{results.map(result => <Pressable key={result.assessmentId} onPress={() => onSelect(result.assessmentId)} style={[styles.card, selectedId === result.assessmentId && styles.selected]}><View style={styles.row}><Text style={styles.name}>{result.expectationId}</Text><Text style={styles.status}>{result.status.replaceAll("_", " ")}</Text></View><Text style={styles.meta}>{result.patientId ?? "Exercise"} · {result.evidence.length} evidence reference(s)</Text></Pressable>)}</View>; }
const styles = StyleSheet.create({ card: { borderWidth: 1, borderColor: "#dfe1e6", borderRadius: 10, padding: 12, marginBottom: 8 }, selected: { borderColor: "#005bbb", backgroundColor: "#f0f7ff" }, row: { flexDirection: "row", justifyContent: "space-between", gap: 8 }, name: { color: "#172b4d", fontWeight: "900", flex: 1 }, status: { color: "#172b4d", fontWeight: "900" }, meta: { color: "#6b778c", marginTop: 5, fontSize: 12 } });
