import type { AnalyticsReport } from "@/models/analytics/Analytics";
import { StyleSheet, Text, View } from "react-native";
export function AnalyticsSummaryCard({ report }: { report: AnalyticsReport }) { return <View style={styles.card}><Text style={styles.title}>Analytics Summary</Text><Text style={styles.meta}>Debrief · {report.sourceDebriefHash.slice(0, 16)}…</Text><Text style={styles.meta}>Analytics · {report.analyticsHash.slice(0, 16)}…</Text><Text style={styles.meta}>Registry · {report.providerRegistryVersion}</Text><View style={styles.row}>{report.categories.map(item => <View key={item.category} style={styles.item}><Text style={styles.category}>{item.category.replaceAll("_", " ")}</Text><Text style={styles.count}>{item.valueCount}/{item.metricCount} values</Text></View>)}</View></View>; }
const styles = StyleSheet.create({ card: { backgroundColor: "#f4f6f8", borderRadius: 12, padding: 14, marginBottom: 12 }, title: { fontWeight: "900", fontSize: 19, color: "#172b4d", marginBottom: 8 }, meta: { color: "#5e6c84", fontFamily: "monospace", fontSize: 11, marginTop: 2 }, row: { flexDirection: "row", flexWrap: "wrap", gap: 8, marginTop: 10 }, item: { backgroundColor: "#fff", borderRadius: 8, padding: 9, flexGrow: 1 }, category: { color: "#172b4d", fontWeight: "800", fontSize: 11 }, count: { color: "#6b778c", marginTop: 3 } });

